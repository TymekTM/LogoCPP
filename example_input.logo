def krzaczek(x, n){
    if (n > 0){
        backward(x);
        left(45);
        krzaczek(x * 0.75, n - 1);

        right(90);
        krzaczek(x * 0.75, n - 1);

        left(45);
        forward(x);
    };
};
krzaczek(50, 6);
